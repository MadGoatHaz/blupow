# Allow all users to connect without a username/password.
allow_anonymous true

# Allow connections from any IP address.
# This is crucial for allowing the test runner on the host to connect.
listener 1883 0.0.0.0 